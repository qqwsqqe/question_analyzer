"""配置文件 - 投资顾问考试题目分析器"""

# 输出配置
OUTPUT_DIR = "output"
OUTPUT_FILENAME = "sorted_questions.pdf"

# 题目解析配置
QUESTION_PATTERNS = {
    # 题号匹配：支持 1.  1、  1.  (1)  第1题 等格式
    'question_number': r'(?:^|\n)\s*(?:第\s*)?(\d+)\s*[\.、\.\)]\s*',

    # 选项匹配：A. B. C. D. 或 A、B、C、D、等
    'option': r'([A-D])[\.、\.\)]\s*([^\n]+)',

    # 答案匹配：答案[:：]\s*[A-D] 或 【答案】\s*[A-D] 等
    'answer': r'(?:答案|【答案】)[:：]?\s*([A-D]+)',

    # 解析匹配 - 匹配到下一道题开始或文本结束
    'analysis': r'(?:解析|【解析】)[:：]?\s*([\s\S]*?)(?=(?:\n\s*(?:第\s*)?\d+\s*[\.、\.\)]|(?:答案|【答案】)[:：]?|$))',
}

# 停用词 - 投资顾问领域常见停用词
STOP_WORDS = {
    '的', '了', '和', '是', '在', '有', '我', '都', '个', '与', '也', '对', '为', '能',
    '很', '可以', '就', '不', '会', '要', '没有', '我们', '这', '上', '他', '而',
    '及', '与', '或', '但是', '因为', '所以', '如果', '那么', '虽然', '但是', '下列',
    '以下', '关于', '有关', '正确', '错误', '说法', '选项', '答案', '解析', '题目',
    '根据', '以上', '所述', '下列', '一项', '属于', '不属于', '主要', '需要',
    '以下', '各种', '有关', '一定', '必须', '应当', '不得', '可能', '应该',
    '投资', '投资者', '证券', '证券业', '以下', '下列', '属于', '关于',
    '一', '二', '三', '四', '五', '个', '种', '年', '月', '日',
}

# 知识点提取配置
KNOWLEDGE_CONFIG = {
    # 提取前N个高频关键词作为知识点
    'top_k': 20,

    # 关键词最少出现次数
    'min_freq': 2,

    # 每个题目最多关联的知识点数量
    'max_knowledge_per_question': 3,

    # N-gram范围
    'ngram_range': (1, 3),
}

# PDF生成配置
PDF_CONFIG = {
    'title_font': 'SimSun',  # 中文字体
    'content_font': 'SimSun',
    'title_size': 16,
    'subtitle_size': 12,
    'content_size': 10,
    'margin': 50,
}
